import React from 'react';

// This component is not used in the Extension version as we interact directly with the YouTube page.
const VideoPlayer = () => {
  return null;
};

export default VideoPlayer;